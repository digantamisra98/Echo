from setuptools import setup

setup(name='Echo',
      version='0.1',
      description='Python package containing all mathematical backend algorithms used in Machine Learning.',
      packages=['Echo'],
      zip_safe=False)
